interface SliderArrowProps {
  direction: "left" | "right";
  onClick?: () => void;
}

export default function SliderArrow({ direction, onClick }: SliderArrowProps) {
  // 52x52 circular button with arrow icon
  // Used in Products slider
  return null;
}
